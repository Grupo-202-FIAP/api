---
spring:
  application:
    name: fastfood
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/fastfood}
    username: ${DB_USER:postgres}
    password: ${DB_PASS:postgres}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
    # ddl-auto: update # create-drop
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
  flyway:
    enabled: true
    classpath: db/migration
    location: classpath:db/migration

  server:
    servlet:
      context-path: /api/v1

  management:
    endpoints:
      web:
        exposure:
          include: health,info,prometheus
    endpoint:
      prometheus:
        enabled: true
    metrics:
      export:
        prometheus:
          enabled: true
api:
  security:
    token:
      secret: ${JWT_SECRET:minha-secret}

mercadoPago:
  publicKey: ${PUBLIC_KEY:APP_USR-af3118dd-c2cf-46fc-8a3e-01e74be4a614}
  accessToken: ${ACCESS_TOKEN:APP_USR-2809902885795734-052201-249bcc45ee8385397056e7d019cd11cd-1363380766}
  clientId: ${CLIENT_ID:2809902885795734}
  clientSecret: ${CLIENT_SECRET:1D7LfKw1COrDlYavbk1TSb85dkDfkSCq}

